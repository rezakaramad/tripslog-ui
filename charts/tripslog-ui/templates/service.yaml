apiVersion: v1
kind: Service
metadata:
  name: {{ include "tripslog-ui.fullname" . }}
  labels:
    {{- include "tripslog-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "tripslog-ui.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
